# 教程 3:旧项目接入(已写了一半的项目,小白版)

> 适用场景:项目**已经存在**(有代码或文档),但从没进过这套"大脑仓库"系统。
> 前提:这台机器已完成《教程 2:新电脑从零配置》。
> 每个项目约 10 分钟,一次接一个,接完它就和新项目没区别了。

## 先判断:你的项目属于哪种?

| 类型 | 特征 | 走法 |
| --- | --- | --- |
| A | 普通文件夹,从没用过 git | 全程照走 |
| B | 本地是 git 仓库,但没推过 GitHub | 跳过第 2 步,做第 5 步 |
| C | GitHub 上已经有仓库 | 跳过第 2、5 步 |

不确定?打开项目文件夹,看有没有隐藏的 `.git` 文件夹;或者直接问 agent:"这个项目是不是 git 仓库?有没有远程?"

---

## 第 1 步:打开项目

DSH 打开这个项目的文件夹(老项目也一样,会话工作目录就是项目文件夹)。

## 第 2 步:交给 git 管(仅 A 类)

对 agent 说:

> **"把这个项目初始化成 git 仓库,并把现有文件做第一次提交"**

(B、C 类跳过这步。)

## 第 3 步:给 agent 补课(旧项目最关键的一步!)

新项目的笔记是边聊边长出来的;**旧项目的历史全在代码里,agent 现在一无所知**。
所以要先让它"预习",对 agent 说:

> **"先通读这个项目的代码和文档,搞清楚它是干嘛的、怎么跑起来、现在做到哪了。
> 然后在大脑仓库的 `项目\` 文件夹下,新建以本项目命名的子文件夹,把 CONTEXT.md(项目说明:是什么/怎么跑/关键文档)
> 和 NEXT.md(当前状态/下一步/已知坑)写成真实情况"**

**判断成功**:打开大脑仓库里 `项目\<项目名>\CONTEXT.md` 读一遍——它写的和你心里想的一致
= 补课成功;哪里不对,直接说"把 CONTEXT 里 XX 部分改成 XX",改到你对为止。

> 这一步值得花耐心:CONTEXT 写得准,以后任何一台机器的 agent 接手都不会跑偏。

## 第 4 步:首次收尾

对 agent 说:

> **"收尾"**

自动:写今天的日志(带 commit 短哈希)→ 校准 NEXT → 推送大脑仓库。
中途问"代码改动要一并提交吗?",答"好"。

## 第 5 步:建云备份(A、B 类需要,C 类跳过)

对 agent 说:

> **"在 GitHub 上建一个叫 <项目名> 的私有仓库,把项目代码推上去"**

(A 机已登录 GitHub CLI,一条命令的事。)

**判断成功**:浏览器打开 `https://github.com/你的用户名/<项目名>` 能看到代码。

## 第 6 步:没有第 6 步

到这里,这个旧项目已经**和新项目完全一样**了——日常就是《教程 1》那套:开发 → "收尾"。

---

## B 机怎么接手(和教程 1 第 3 部分相同)

1. B 机 `git pull` 大脑仓库(这个项目的笔记自动到达)
2. B 机 clone 项目代码仓库(第一次)
3. 对 agent 说:"先读大脑仓库里 <项目名> 的笔记,接着干"

---

## 注意事项

- **三方同名**:本地项目文件夹名 = GitHub 仓库名 = 大脑仓库 `项目\` 下的文件夹名。skill 靠本地文件夹名认路,想改名就三处一起改。对不上时 skill 会**先近似扫描并问你**(含远程仓库名比对),确认不是重复才新建——不会默默建出第二个文件夹。
- **一次接一个**:别一口气接十个项目;接完一个,收尾验证一个。
- **补课质量你把关**:CONTEXT 就是这个项目以后的"户口本",花两分钟读一遍,值得。
- **C 类项目想换私有/换仓库地址**:让 agent 处理 `git remote set-url`,别手动乱删。
