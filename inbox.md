# Inbox — 随手想法

> 还没长成项目的灵感先扔这里;成熟了就升级成 `./项目/<项目名>\` 文件夹。

- 2026-09-22 DevHandoff 的 MCP Server 未来可以直接把本仓库当数据源:
  CONTEXT/DECISIONS/NEXT/JOURNAL 的结构就是 `devhandoff_get_project_context`
  需要的 Markdown 形态。MVP 做出来之前,本仓库就是"手动版 DevHandoff"。
