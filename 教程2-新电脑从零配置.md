# 教程 2:全新电脑从零配置(主力机方向,小白版)

> 适用场景:一台**从没配过**这套系统的电脑,你想把它变成主力开发机(A 机角色)。
> 配好之后的日常使用,看《教程 1:日常开发全流程》。
> 全程约 15~30 分钟,绝大多数步骤是"复制粘贴 + 点下一步"。
> 说明:本系统 GitHub 上已经有大脑仓库,所以本教程的主线是"新电脑加入现有系统";
> 如果你面前是全世界第一台电脑(还没有大脑仓库),直接跳到文末【附 A】。

## 第 0 章:装软件

### 0.1 Git(必须)

打开 PowerShell(开始菜单搜 powershell),粘贴回车:

```powershell
winget install --id Git.Git -e
```

装完**关掉 PowerShell 重新打开**,敲 `git --version`,显示版本号 = 成功。
(没有 winget 的老系统:去 `git-scm.com` 下载安装包,一路默认下一步。)

### 0.2 DSH(必须)

按你平时获得 DSH 的方式安装并登录。

### 0.3 GitHub CLI(强烈推荐)

有了它,以后才能"一句话建仓库"(说"收尾"、建仓推送都靠它):

```powershell
winget install GitHub.cli
```

装完重开 PowerShell,登录:

```powershell
gh auth login
```

依次选:`GitHub.com` → `HTTPS` → `Login with a web browser` → 浏览器里登录你的 GitHub 账号并授权。
验证:`gh auth status` 显示 `Logged in to github.com account 你的用户名` = 成功。

### 0.4 Obsidian(可选)

给你自己舒服翻笔记用的,不装不影响任何功能。`obsidian.md` 下载,打开时选大脑仓库文件夹。

## 第 1 章:告诉 git 你是谁(必须,一次)

```powershell
git config --global user.name  "你的名字(英文或拼音)"
git config --global user.email "你GitHub账号的邮箱"
```

## 第 2 章:领取大脑

```powershell
git clone https://github.com/ljr282341583/agent-brain.git "$env:USERPROFILE\agent-brain"
```

- 私有仓库会弹 GitHub 登录,登一次以后免登
- clone 到哪都行(放哪都通用),**记住这个路径**,下一步要用

## 第 3 章:三件配置(懒人路径)

DSH 新开会话,对 agent 说:

> **"读 <刚clone的路径>\README.md,按『新机器初始化清单』把跨机接力配置好"**

它自动完成三件事:

1. 把 `skills\session-handoff\` 复制到 `%USERPROFILE%\.agents\skills\session-handoff\`(装"收尾"技能)
2. 把 README 里「全局约定」代码块写进 `%USERPROFILE%\.dsh\AGENTS.md`(开局自动读笔记的约定)
3. 把本机 clone 路径写进 `%USERPROFILE%\.agent-brain`(大脑在哪,本机记住了)

<details><summary>想手动做?三件事的原理(点开)</summary>

```powershell
# 1. 装 skill(目录不存在就先建)
New-Item -ItemType Directory -Force "$env:USERPROFILE\.agents\skills" | Out-Null
Copy-Item "<clone路径>\skills\session-handoff" "$env:USERPROFILE\.agents\skills\session-handoff" -Recurse -Force

# 2. 全局约定:把 README「全局约定」代码块的内容,保存为 %USERPROFILE%\.dsh\AGENTS.md(没有该文件就新建)

# 3. 大脑位置:
Set-Content "$env:USERPROFILE\.agent-brain" "<clone路径>" -NoNewline
```

</details>

## 第 4 章:重启 + 验收

1. **重启 DSH 会话**(技能要重启才加载)
2. 问:**"你有 session-handoff 技能吗?"** → 答"有"
3. 问:**"大脑仓库在哪?里面有哪些项目?"** → 能答出位置和项目名

三条全过 = **这台机器毕业了** 🎓

## 第 5 章:开始干活

- 想当主力开发机(A 机角色)→ 直接进《教程 1》第 1 部分:开新项目
- 只当接力机(B 机角色)→ 进《教程 1》第 3 部分:clone 项目代码 → 读笔记 → 开工

---

## 附 A:全世界第一台机器(GitHub 上还没有大脑仓库)

两条路,选顺手的:

1. **有旧电脑**:把大脑仓库整个文件夹拷过来(U 盘/网盘)→ GitHub 网页 New repository 建一个**私有空仓库**(名字 agent-brain,**什么都不要勾选初始化**)→ 在文件夹里:
   ```powershell
   git remote add origin https://github.com/你的用户名/agent-brain.git
   git push -u origin main
   ```
2. **彻底从零**:手动建文件夹,让 agent 照着 README 的「目录结构」生成最小骨架(README / machines.md / inbox.md),再:
   ```powershell
   gh repo create agent-brain --private --source . --push
   ```

## 附 B:常见报错对照

| 报错/现象 | 原因与解法 |
| --- | --- |
| `'git' 不是内部或外部命令` | Git 没装好,或装完没重开 PowerShell |
| `Repository not found` | GitHub 上还没有这个仓库,或登录的不是拥有它的账号 |
| push 被拒(fetch first) | 远端有新东西,先 `git pull --rebase` 再 push |
| clone 时卡在登录 | 浏览器弹窗登录 GitHub 并授权;登错了就到 Windows"凭据管理器"删掉 GitHub 凭据重来 |
| 说"收尾"没反应 | 会话没重启(技能没加载),或第 3 章没做 |
