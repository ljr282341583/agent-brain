# 教程 1:日常开发全流程(小白版)

> 适用对象:**已经配置好**这套系统的机器(A 机、B 机都算)。
> 还没配置?先看《教程 2:新电脑从零配置》。
> 这份教程本身放在大脑仓库里,跟着 git 走,每台机器都自带一份。

## 开胃:认识三个词

| 词 | 意思 |
| --- | --- |
| 仓库 | 被 git 管理的文件夹,可以推送云备份(GitHub),也能从云端下载回来 |
| 大脑仓库 | 专门装**思路笔记**的仓库,名字叫 `agent-brain`,全网只有一个 |
| 项目仓库 | 装某个项目**代码**的仓库,每个项目一个 |

一条铁律:**你只负责说话,git 命令全是 agent 的活。**
教程里给出的命令只是让你知道背后发生了什么,能不敲就不敲。

---

# 第 1 部分:A 机开新项目(以 `new-app` 为例,名字换成你的)

## 第 0 步:一次性(每台机器只做一次)

1. **重启 DSH 会话**(关掉对话重开)——让"收尾"技能生效
2. 新会话里问一句:**"你有 session-handoff 这个技能吗?"** → 答"有" = OK

## 第 1 步:建项目

DSH 里新建项目(选个文件夹,比如 `...\workspace\projects\new-app`),然后对 agent 说:

> **"把这个项目文件夹初始化成 git 仓库"**

(10 秒的事,它自己敲命令。)

## 第 2 步:正常开发

聊需求、写代码、改 bug——**和以前没有任何区别,你不用管笔记的事**。

## 第 3 步:今天结束了

对 agent 说一个词:

> **"收尾"**

它会自动:写今天的开发日志 → 更新断点看板(NEXT)→ 推送到 GitHub 大脑仓库。
中途可能问你"这些代码改动要一并提交吗?",答"好"就行。

**判断成功**:它汇报里出现"大脑仓库已推送"和一串短哈希(如 `a1b2c3d`)。

## 第 4 步:给项目建云备份(每个项目只做一次)

对 agent 说:

> **"在 GitHub 上建一个叫 new-app 的私有仓库,把项目代码推上去"**

(A 机已登录 GitHub CLI,它一条命令搞定。)

**判断成功**:浏览器打开 `https://github.com/你的用户名/new-app` 能看到代码。

**到这里,A 机的日常定型:以后每天就两个动作——开发 → 说"收尾"。**

---

# 第 2 部分:B 机第一次(一次性,约 10 分钟)

前提:B 机装好 **DSH** 和 **Git**(没装 Git 先装,装完重开窗口)。

## 第 1 步:把大脑下载下来

打开 PowerShell(开始菜单搜 powershell),粘贴回车:

```powershell
git clone https://github.com/ljr282341583/agent-brain.git "$env:USERPROFILE\agent-brain"
```

- 弹出 GitHub 登录窗口 → 登录你的账号 → 等下载完
- **想放别的盘?** 把结尾换成比如 `"D:\ai\agent-brain"`,放哪都行,系统会自动记住位置

## 第 2 步:让 B 机的 agent 自己配好一切

打开 B 机 DSH,新开会话,说:

> **"读 <刚clone的路径>\README.md,按『新机器初始化清单』把跨机接力配置好"**

它自动完成:装"收尾"技能 → 写开局约定 → 记住大脑仓库在哪。等它说"配好了"即可。

## 第 3 步:重启会话 + 验收

重启 DSH 会话,问它:

> **"DSH双击协作 这个项目现在做到哪了?下一步是什么?"**

能答出断点内容 = **B 机已经有脑子了,全套流程打通** 🎉

---

# 第 3 部分:B 机接手干活(每个项目第一次)

## 第 1 步:把项目代码下载下来

打开项目在 GitHub 的网页 → 绿色 **Code** 按钮 → 复制地址,然后 B 机 PowerShell:

```powershell
git clone https://github.com/你的用户名/new-app.git
```

(嫌麻烦也可以直接对 B 机 agent 说:"帮我从 GitHub clone new-app 项目"——它会**先问你放哪个路径**,这是"不另建克隆"的约定,答个路径即可)

## 第 2 步:开工

DSH 打开这个项目文件夹,说:

> **"先读大脑仓库里 new-app 的笔记,接着上次的进度继续"**

它自己去读,带着 A 机留下的全部思路干活——这就是"B 机看到 A 机的想法"。

## 第 3 步:结束

说 **"收尾"**。第一次它会问"这台机器叫什么代号?" → 答 **"B机"**(只问这一次,终身记得)。

## 第 4 步:过几天回 A 机

新会话说:**"把大脑仓库和项目仓库都 pull 一下,接着 B 机的进度干"** —— 完美闭环。

---

# 速查表(背诵版)

| 场景 | 你要说的全部 |
| --- | --- |
| A 机开新项目 | "init 一下" → 开发 → **"收尾"** → "GitHub 建仓推送" |
| A 机日常 | 开发 → **"收尾"** |
| B 机第一次 | clone 大脑 → 一句话配置 → 重启 → 验收(见第 2 部分) |
| B 机日常 | "pull 一下,读笔记接着干" → 开发 → **"收尾"** |
| 灵感闪现(还没项目) | 对 agent 说:"丢进 inbox" |

---

# 常见问题

- **说"收尾"没反应?** 会话没重启、技能没加载;或者这台机器还没做初始化(看教程 2 第 3 章)。
- **push 被拒 / 冲突?** agent 会自动 `pull --rebase` 重试;它搞不定会停下来问你,不会弄丢东西。
- **想舒服地翻笔记?** 用 Obsidian 打开大脑仓库文件夹,搜索、双链随便用;手机装 Obsidian 也行。
- **同一天收尾好几次?** 日志自动追加小节,不会覆盖。
- **笔记会不会越堆越乱?** 不会——NEXT 只留当前断点,旧内容挪进日志;红线见 README。
- **又买了一台新电脑?** 看教程 2,clone + 一句话配置,15 分钟加入。
