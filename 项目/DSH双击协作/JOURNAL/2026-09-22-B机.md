# 2026-09-22 — B机(WIN-20251127LIT)

## 本次做了什么

- B 机按大脑仓库 README「新机器初始化清单」完成跨机接力配置(三件事):
  1. `%USERPROFILE%\.agent-brain` 写入本机 clone 路径(指向 `G:\ai\agent-brain`);
  2. 仓库内 `skills\session-handoff\` 复制到 `%USERPROFILE%\.agents\skills\session-handoff\`(与仓库副本文件哈希一致);
  3. 「跨机接力约定」写入 `%USERPROFILE%\.dsh\AGENTS.md`。
- 顺手清理:删除本机重复 clone(`%USERPROFILE%\agent-brain`)。删除前校验过:工作区干净、无未推送提交、HEAD 与保留副本同一 commit(`9fe1ed2`),删掉不丢任何东西。
- 代号登记:本机 `COMPUTERNAME = WIN-20251127LIT`,经用户确认登记为 **B机**。

## 关键结论 / 决策摘要

- **验证方式(skill 生效不需要重启)**:三项配置写入后,同一会话内全局 AGENTS.md 已被加载、`session-handoff` 已进入 skill 目录 —— skill 根目录被 watch,根目录不存在时会被探测,新建即生效。以后新机器初始化完可直接当场验证,不必等重启。
- 清单里"三件套"的位置与 DSH 实际读取路径核对一致:`AGENTS.md` 读 `$DSH_HOME(~/.dsh)/AGENTS.md`;skill 读 `~/.agents/skills`(rank 500,低于 `~/.dsh/skills` 的 rank 400)。
- 新发现一处会导致笔记分家的命名不一致,已按用户决定"先记录、暂不改名",详见 `../NEXT.md` 已知坑与 `../DECISIONS.md`。
- 项目代码仓库仍未建;本机工作目录 `DSH双机协作` 目前只有 `PRD-DevHandoff.md`,且尚未 `git init`。

## 遗留 / 交给下一台机器

- A 机:重启会话验证 `session-handoff` 出现,并试跑首次"收尾"(NEXT.md 下一步第 1 条)。
- 两台机器都注意:`DSH双机协作` / `DSH双击协作` 命名不一致未解决,收尾前先看 NEXT.md 已知坑。
- 下次收尾时,若 B 机角色需要更精确的描述(主力 / 副机),请顺手改 `../machines.md` 本机那一行。
