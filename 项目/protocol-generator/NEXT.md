# NEXT — protocol-generator(人民调解协议生成器)

> 断点唯一真相源。收尾必更新;过时事项挪进当篇 JOURNAL,不堆积也不删历史。
> 最近更新:2026-09-22 A机

## 进行中

- **v5.9.2 已完成、待验收发布**:参加人自动带入功能 + 15 项回归测试全过,成品 `人民调解协议生成器启动码v5.9.2.html` 与 `_test_attendees.js` 只在 A 机工作区根目录(**git 仓库外**);项目仓库 main `822533a` 与远程一致、工作区干净。

## 下一步(按顺序)

1. 用户浏览器验收 v5.9.2:第 5 步填调解员/记录人 → 全套卷宗看《人民调解调查记录》《人民调解记录》参加人行(同名/异名/未填三态 + 下划线)→「批量导出全部卷宗」核对 Word 一致。
2. 验收通过即发布:复制成品进仓库 `方案一启动码/`,`_test_attendees.js` 是否入库听用户定,README 版本表 + 更新日志补 v5.9.2 条目,提交并推送项目仓库。
3. 旧候选(均待用户拍板):方案二文件名 v6.0 vs 内容 V6.3 整理;密码授权版 v5.1 去留;V6.3 时代写的 `设备绑定/Supabase配置指南.html` 有效性核验。
4. 导出类改动继续沿用「Node + vm 提取页面真实函数」验证再发布,样例:`_test_attendees.js`。

## 已知坑

- **v5.9.2/测试脚本在 git 仓库外**:工作区根的散件(含 v5.9.1 等副本)不受 git 保护,换机器/清工作区会丢;接力第一件事是取回 v5.9.2 成品与 `_test_attendees.js`。
- **测试桩必须复刻真实语义**:本次自制 `underline` 桩漏了「空值→占位线」逻辑造成 3 项假失败;优先从 HTML 提取真实函数注入,不要手写桩。
- **版本号混乱(仍未整理)**:方案二文件名叫 v6.0、内容实为 V6.3(与 `历史版本\V6.3授权码版\` 哈希一致);README 版本演进表非严格时间序;判断功能一律以 README 更新日志为准(v5.9.1 title 遗留已随 v5.9.2 修正,见 JOURNAL)。
- **老内核兼容是雷区**:Chromium 80~102 不支持 `deflate-raw`,docx 导出必须走 `dsh-compat` 块的 puff 兜底;删掉兜底或只在新内核测过就发布,v5.5.1 修过一次这种事故。
- **顶层函数重名覆盖**:v5.5 曾因 `renderMediationRecord` 与调解记录表模块函数重名,后声明覆盖前声明,导致全套卷宗"人民调解记录"页 undefined。新增顶层函数前先全局搜重名。
- **旧测试脚本硬编码路径**:`_test_certtype.js` 指向已不存在的 `协议生成修改/`,复用前须改路径;`verify-dsh-export.js` 针对 v5.5,与新版本可能不对应。
- **启动密码已公开**:默认密码明文写在公开仓库 README(按红线笔记不复述);要收紧授权就改 HTML 内 `DEFAULT_PASSWORD` 并同步删 README 明文。
- **单文件体量大**:最新版 HTML 300KB+、内联脚本数千行,修改务必另存新版本文件,不原地覆盖已发布版本;README 更新日志必须同步。
