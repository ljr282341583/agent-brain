# NEXT — protocol-generator(人民调解协议生成器)

> 断点唯一真相源。收尾必更新;过时事项挪进当篇 JOURNAL,不堆积也不删历史。
> 最近更新:2026-09-22 A机

## 当前状态(2026-09-22)

- **最新发布 = 方案一 v5.9.1**(2026-09-16,远程提交 `822533a`):修复「导出 Word(协议书)」「批量导出全部卷宗」「批量导出个人卷宗」生成的 .docx 损坏问题——改为标准 OOXML 容器嵌入 v5.8 的完整 HTML 内容与样式。该提交是**网页端直接上传**的,当时 A 机本地落后 1 个提交。
- **本次会话动作**:A 机已 `git pull` 同步到 `822533a`(工作区干净、与远程一致);通读全部代码与文档;在大脑仓库新建本文件夹并写入 CONTEXT/NEXT。**代码零改动**。
- 无未提交改动、无挂账 bug。方案一主线 v5.7/v5.8/v5.9/v5.9.1 并存保留;方案二 = V6.3 内容;密码授权版停 v5.1。

## 下一步(仓库内无既定路线图,以下为观察所得候选,均待用户拍板)

1. **小修**:v5.9.1 文件 `<title>` 仍写着「人民调解协议生成器 v5.9」,下次改版顺手改成 v5.9.1。
2. **方案二版本命名整理**:文件名恒为 `…v6.0_方案二_设备绑定.html`,内容已迭代到 V6.3;建议要么改名对齐,要么在 README 顶部加一句"文件名不代表版本号"。
3. **密码授权版去留决策**:停在 v5.1,没有 v5.7 证件校验、v5.9 草稿/备份;若仍对外分发需决定是否把主线功能同步进去,不再分发则建议明确标注归档。
4. **方案二配置指南有效性核验**:`设备绑定/Supabase配置指南.html` 写于 V6.2 时代,V6.3 改成"同码同设备严格监测、导出立刻下线"后,指南步骤是否仍适用未验证。
5. 若做导出类改动:沿用「Node + vm 提取页面函数」的验证思路(见已知坑里的脚本位置),先跑通再发布。

## 已知坑

- **版本号三重混乱**:① 方案二文件名叫 v6.0,内容实为 V6.3(与 `历史版本\V6.3授权码版\` 内文件哈希一致),判断功能以 README 更新日志为准;② README 版本演进表顺序是按行文需要排的,不是严格时间序;③ v5.9.1 的 title 未更新(见下一步 1)。
- **老内核兼容是雷区**:Chromium 80~102 不支持 `deflate-raw`,docx 导出必须走 `dsh-compat` 块的 puff 兜底;删掉兜底或只在新内核测过就发布,v5.5.1 修过一次这种事故。
- **顶层函数重名覆盖**:v5.5 曾因 `renderMediationRecord` 与调解记录表模块函数重名,后声明覆盖前声明,导致全套卷宗"人民调解记录"页 undefined。新增顶层函数前先全局搜重名。
- **测试脚本不在仓库**:`verify-dsh-export.js`(v5.5 导出链路端到端验证)、`_test_certtype.js`(v5.7 证件类型)散落在 A 机 DSH 工作区根(`…\workspace\projects\protocol-generator\`,git 仓库的上一级),未入库;且 `_test_certtype.js` 硬编码了旧路径 `协议生成修改/`(已不存在),复用前须改路径。另有一份解包 docx 目录 `_测试解包\` 与若干 HTML 工作副本(工作区根的 v5.9 副本与仓库版哈希一致)。
- **启动密码已公开**:默认密码 `2025mediation` 写在公开仓库 README 里;若要收紧授权,改 HTML 内 `DEFAULT_PASSWORD` 并同步删掉 README 中的明文。
- **单文件体量大**:最新版 HTML 300KB+、内联脚本数千行,修改务必「另存新版本文件」,不要原地覆盖已发布版本;README 更新日志必须同步。
