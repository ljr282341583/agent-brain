# 2026-09-23 — B机

## 本次做了什么

- 升号 0.3.6 并落地 NEXT 第 2 条：`build`/`build:dir` 前置自动跑 `prepare-runtime.ps1`
  （脚本加幂等快路径，runtime 就绪时秒过不重下）；smoke [2] 静态检查新增「侧车含可执行
  npm」断言（npm-cli.js 存在 + 侧车真跑 `--version` 出版本号）；双 README 同步说明。
- 修 B机 dev 树腐坏：`app\node_modules` 停在 dsh 0.1.1-rc.2（lockfile 是 0.1.5-rc.1），
  `npm ci`（ELECTRON_MIRROR=npmmirror，34s/880 包）修复——此前造成三连假故障：
  smoke [3] 120s 等不到 token URL、[4] 打包 exe 秒退 0、real-home 启动崩。
- 修 `prepare-runtime.ps1` 编码：BOM-less UTF-8 被 PS5.1 按 GBK 解析报
  `Unexpected token '}'`，重写为带 BOM 后 exit 0。
- 本地 `npm run build` 门禁全绿：通过 7、失败 0、跳过 1（[5] 因 4 个运行实例按设计
  SKIP）；Setup 0.3.6.exe / portable / win-unpacked 已刷新。
- 「损坏会话」查案并处置：不是文件损坏——项目文件夹改名（协议生成修改→
  protocol-generator）+ 09-19 会话目录批量搬迁，仅 v2 旧代 header 户口仍指老路径。
  复刻 `assertStoredIdentity` 路径推导做离线审计：82 会话中 80 一致、2 个 v2/v3
  代际户口不一、0 整体错位；0.1.5 启动 `listArtifacts` 只选最高代（v3，户口本来就对），
  v2 永不被读。
- 处置拍板改道（原「挪回原位」被审计证伪）：2 个陈旧 v2 文件隔离到
  `C:\Users\Administrator\.dsh\quarantine-legacy-v2\`（保留原相对路径结构），v3
  原地不动，复审 82/82 全绿；审计脚本入库
  `过程记录\2026-09-23-会话档案身份审计.js`（系统 node 直接跑）。
- 清理手动复现残留：侧车 PID 4464、smoke-manual-home；端口 3199/3490 已清。
- 上轮「0.3.5 收官、反馈三件套待下一版带出」清账：反馈三件套代码已在库，随本次
  0.3.6 tag 带出。

## 关键结论 / 决策摘要

- 本次新增 DECISIONS 两条：build 自动前置 prepare-runtime + 侧车 npm 断言；
  损坏会话隔离不回迁。详细决策见 DECISIONS.md。
- 项目代码分支/commit：main 7ef9d69（本次收尾提交随后推送，哈希见文末追记）。

## 遗留 / 交给下一台机器

- **0.3.6 已提交推送、tag 未打**：说「打 v0.3.6 标签」→ `-F` notes 文件
  （UTF8 无 BOM）push 触发 release.yml 自动发布 → 托盘/更新验证。
- [5] 安装 E2E 欠着：退出全部正式实例后终端 `cd app && npm run verify:smoke` 可补全
  （[5] 装临时目录并暂存/导回卸载键，真安装安全）。
- 本机装机 0.3.4，发版后可升 0.3.6（更新向导保持全机模式）。
- 会话档案修复终验 = 重启桌面端应干净启动；回滚点 = quarantine 目录按原相对路径
  搬回（详见 NEXT 已知坑 quarantine 条）。
- 收尾档位二复跑 `npm run build` 与收尾提交哈希见文末追记。

## 收尾追记（2026-09-23 B机）

- 项目仓库收尾提交 `02ff72f`（9 文件：0.3.6 改动 6 + 08-23 过程记录 2 + 审计脚本 1），
  已推送 `origin/main`（7ef9d69..02ff72f），工作区干净。
- 档位二复跑 `npm run build`：exit 0，**通过 7、失败 0、跳过 1**（[5] 因 4 个运行实例
  按设计 SKIP）。prepare-runtime 幂等快路径生效（「侧车运行时已就绪…跳过下载解压」），
  smoke [2] 新断言「侧车含可执行 npm」通过，dsh 守卫 0.1.5-rc.1 对齐；产物
  Setup 0.3.6.exe / portable / win-unpacked 已刷新。
- **v0.3.6 已打标并推送**（tag → `9c72f2c`，notes 源 `docs/release-notes-v0.3.6.md`
  先行单独提交），release.yml 自动发布已触发。实测：`git tag -F` 会剥掉一切 `#` 开头
  的行，故 GitHub Release 描述为无节标题的正文（与 0.3.5 同样式）；需要节标题可编辑
  Release 描述、粘贴 docs 里 notes 文件全文（带 `## ` 的 Markdown 版）。
- **发版+升级闭环（同日深夜）**：Actions 发布成功，用户经托盘「检查桌面端更新」自动升到
  0.3.6（`D:\AI(CODEX)` 单点安装，无 G:\ 副本、无双安装；反馈三件套实战走了一遍）；
  壳 23:17 冷启、侧车 23:30 在隔离后的真实 `~/.dsh` 上冷起、会话续命——**会话档案修复
  终验通过**。NEXT 下一步原 1（发版验证）/3（升级）/4（终验）三条随之闭环。
- **[5] 补跑全绿（同日深夜，用户手动执行）**：退出桌面端实例后 `npm run verify:smoke`
  → **通过 8、失败 0、跳过 0**。[5] 实测：静默装临时目录（223s）→ 3590 端口真监听
  → 卸载器退出码 0、文件移除 → 快捷方式还原，真安装 0.3.6 全程未受影响。0.3.6 至此
  为五步体检全绿收官版本，NEXT 下一步第 1 条（补 [5]）闭环。
- **[5] 后"快捷方式丢失"报障核查 = 虚惊（同日深夜）**：用户按每用户位置自检
  （USERPROFILE 桌面/APPDATA 开始菜单/HKCU/LOCALAPPDATA/Program Files）五处全空而误判。
  实测全机安装四件套俱全：D:\ 程序 0.3.6、Public 桌面与 ProgramData 开始菜单两处 lnk
  均有效指向 D:\、HKLM 卸载键 0.3.6、记忆键指 D:\、4 实例在跑。[5] 快照/还原只作用于
  每用户位置且与其临时安装作用域匹配，本次未碰全机安装的任何东西。自查位置坑与
  perMachine 扩展提醒已记 NEXT。
