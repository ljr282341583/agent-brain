# 2026-09-25 — B机

## 本次做了什么

- **修掉「应用内 dsh 更新激活后秒回退」bug 并发布 v0.3.7**。根因：`app/src/main.js` 的
  `child.on('exit')` 不区分退出的是哪个子进程 —— 更新流程是「先 activate 新版本 →
  再 stopHarness 杀旧进程 → 才 startHarness 拉起新版本」，旧进程的 exit 事件迟到时，
  回调读到 `activeVersion` 已有值，就把这次退出误判成「覆盖版本启动失败」，1ms 内回退内置版。
  **该功能自 v0.3.0 起从未成功过一次**（09-12、09-20×2、09-24 共四次同因失败）。
  修法 = exit 回调加 `if (dshChild !== child) return;`（放在关日志 fd 之后、`dshChild = null`
  之前；`dshExitPromise` 用的是独立 `child.once('exit')`，不受影响）。
- **dsh 本体已升到 0.1.7-rc.2**。因为回退 bug 挡路，走的是**手工激活**：改
  `dsh-update\state.json` 的 activeVersion + **完全退出冷启动**（冷启动无旧进程可杀，绕开竞态），
  并用壳自身 `updater.js` 验证 `resolveActiveRoot` 命中该版本。`~/.dsh` 已先备份
  （`.dsh.bak-20260924-230423`，13186 文件 / 283 MB，关键数据逐项核对一致）。
- **排掉 dsh 0.1.7 的两个兼容问题**：① 0.1.7 把 `@deepseek-ai/dsh-settings` 的
  `SettingsProvider` 换成 `SettingsForms`、**移除了浏览器侧 `settingsScope` 服务**，旧版
  `@mars-sea/dsh-commandcode-provider`（早于 0.11.9）inject 它 → 条目永久 pending、页面打不开，
  升到 0.11.11 解决；② `profiles\web\cordis.patch.yml` 的 `defaultPreset: auto` 在 0.1.7
  必定抛错（构造函数里就 resolve，而 `auto` 只在 Auto 审阅集成 live 时才入名单），改为
  `workspace-write`。
- **插件补升**：`dshmarket` 1.47.0 → 1.58.0、`deepseek-harness-wallet` 0.3.1 → 0.3.13。
  拿不到各自最新的 1.65.1 / 0.11.14 —— pnpm 11 默认 `minimumReleaseAge=1440`（1 天供应链冷却）。
- **实机验证修复有效**：点托盘「重启服务」（正是原来必掉版本的场景），版本没掉、
  `state.json` 的 activeVersion 保持 0.1.7-rc.2、日志中「覆盖版本启动失败，回退内置版本」
  不再新增（历史共 5 次，最后一次在修复前）。
- **v0.3.7 发版**：本地 `npm run build` 通过 7 / 失败 0 / 跳过 1 → 提交 `bdf0bb8` → push →
  tag `v0.3.7` → CI 全绿（01:21），正式 Release 非 Draft、四资产齐全、正文取 tag 注释完整写回、
  线上 `latest.yml` = `version: 0.3.7` 连字符名。

## 关键结论 / 决策摘要

- 本轮拍板项见 DECISIONS.md（手工激活 + 热补丁、发版 0.3.7、不绕过 pnpm 冷却期、权限预设取保守值）。
- 项目代码：分支 `main`，提交 `bdf0bb8`（已推送，工作区干净）。
- **壳的两道保险盖不住「插件不兼容」这一类失败**：启动冒烟验证用干净的临时 `DSH_HOME`、
  不带第三方插件；自动回退只在**子进程退出**时触发，而插件 pending 时进程活着、UI 也打得开。
  → 升级 dsh 前后都要先 update 插件。

## 遗留 / 交给下一台机器

- **本机壳仍是 0.3.6**：托盘「检查桌面端更新」升到 0.3.7（向导**保持默认全机模式**，
  别切「仅当前用户」）。装完会覆盖打在已安装体上的 hot patch —— 内容相同，属预期。
- 冷却期过后补升 `dshmarket` 1.65.1 与 `@mars-sea/dsh-commandcode-provider` 0.11.14
  （两者均 2026-09-24 发布）；命令 `dsh plugin --profile web update <pkg>@latest`。
- `dshmarket` 1.58.0 的 `client.js` 仍有 `ctx.inject(["settingsScope"], …)` —— 惰性注入，
  不阻塞启动、只静默失效，升级后观察。
- 备份可择机清理：`~/.dsh.bak-20260924-230423`（283 MB）、`cordis.patch.yml.bak-dsh017fix-*`、
  `main.js.bak-dsh017fix-20260924-232831`、`state.json.bak-20260924-230555`。
- 排查入口：`%APPDATA%\ds-harness-desktop\dsh-desktop.log`（壳，带时间戳）、
  `dsh-child.log`（dsh 子进程 stdout/stderr）、`dsh-update\state.json`（覆盖版本激活状态）。
