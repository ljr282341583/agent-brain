# 2026-09-23 — A机

## 本次做了什么

- 给项目建起 `verify:smoke` 五步冒烟体检（新文件 `app/test/smoke.test.js`）：离线单测 24 项 / 静态检查（侧车 Node≥22、dsh 版本精确钉死、compression:none 双守卫）/ 侧车启动冒烟 / win-unpacked 启动冒烟 / 安装器端到端（静默装→启动→监听→卸载→快捷方式还原）；全程临时 DSH_HOME，不碰真实 ~/.dsh。
- 打包命令挂体检：`npm run build` 与 `build:dir` = electron-builder && smoke `--packaged`；完整 build 已实测全链路通过（NSIS 安装包 + 便携版产出、失败 0）。
- 修打包链路存量 bug：Node 24 禁止裸 spawn `.cmd`（EINVAL，status=null 无输出），当时本机 `npm run build` 必挂；afterPack 回退顺序改为 侧车 npm → 系统 node+npm-cli.js → cmd /c，并补打 result.error（此前诊断被吞）。
- `main.js` 加三个仅 env 生效的验证隔离开关：`DSH_DESKTOP_PORT` / `DSH_DESKTOP_USER_DATA` / `DSH_DESKTOP_SMOKE`（不抢单实例锁），日常行为零变化。
- 事故与修复：[5] 首跑时 NSIS 模板按进程名 `taskkill /im` 连坐杀了用户正在用的 desktop 实例（模板 `allowOnlyOneInstallerInstance.nsh`）；已加守卫（检测到正式实例即 SKIP，实测生效），并实现快捷方式快照/还原防破坏桌面图标。
- `session-handoff` skill 升 1.0.4：新增第 6.5 步收尾打包（仅 ds-harness-desktop），激活版与 brain 分发版同步（哈希一致），registry 已登记。
- 摸底新发现（未处理，见 NEXT）：应用内 dsh 更新链路因侧车无 npm 实际不可用；`docs/分发说明.md` 称"侧车含 npm"与现实矛盾；代码注释引用的 `app/README.md` 不存在。

## 关键结论 / 决策摘要

- 本次三条拍板详见 DECISIONS.md（verify:smoke 门禁 / 档位二仅限本项目 / 开发模式省事路线）。
- 项目代码起点 commit：main `88091a6`（v0.3.4），本次改动随收尾提交。
- 实测数据：完整 build 2~4 分钟；smoke 中 [3] 约 7s、[4] 约 9s；[5] 全量约 3 分钟但尚未拿到完整实证。
- 旧 NEXT 中"下一步方向未拍板"已解决：方向 = 验证基建（verify:smoke），本轮已落地。

## 遗留 / 交给下一台机器

- [5] 全量实证未拿到：desktop 模式死结（壳开着守卫跳过 / 关壳 agent 断电），发版前关壳由用户手动跑 `cd app && npm run verify:smoke`，期望"通过 7 失败 0"。
- 应用内 dsh 更新链路不可用待拍板（三选一，见 NEXT 第 2 条）。
- 版本仍 0.3.4，发 0.3.5 前记得升号（skill 第 6.5 步会提醒）。
- 接力话术不变：新会话对 agent 说"先读大脑仓库里 ds-harness-desktop 的笔记再继续"。

## 第二次收尾 03:24

### 本次做了什么

- 发版前置三件事：升号 0.3.5（package.json + lockfile）、分发说明六处如实化（文件名改实际产物名、删「侧车含 npm」、两处「检查 dsh 更新」标暂不可用、版本表翻新）、新增 release notes 草稿；提交 `f174b90` 已推送。
- [5] 安装器端到端拿到首个完整实证并多轮复跑，最终稳定「通过 7，失败 0，跳过 0」。
- 【重大事故】[5] 曾把本机真实安装连坐卸毁（安装目录清空、双快捷方式被删）。根因两层：NSIS 安装器 `uninstallOldVersion` 按注册表先跑旧版卸载器；卸载器未带 `_?=` 会复制到 %TEMP% 异步执行、在还原快捷方式之后才补刀。
- 修复四件套逐一实测生效：装前暂存/装后导回卸载键（防连坐）、卸载加 `_?=` 同步（实测 4s 真删完）、finally 不再顶掉主错误且清理失败只告警、清理前扫泄漏进程+清只读位+带重试；新增硬断言「真实安装体完好」（首版正则没匹配上、断言哑火，复跑修正后证实真会响）。
- 真实安装已重装修复回原路径 `G:\ai\dsh desktop\DS Harness Desktop`，双快捷方式与注册表键全部回归；`~/.dsh` 数据全程未伤。
- 插曲与发现：NSIS `/D=` 遇含空格路径会被按空格截断，须把整个 `/D=...` 作为单个带引号参数传入（WMI Create 实证可行；误装进 `G:\ai\dsh` 的残留由下一次安装的 uninstallOldVersion 自动清净）。本会话实际由全局 `dsh web`（3080）承载——壳与真安装被毁时会话因此幸存，印证 DECISIONS 中网页版形态抗壳事故的优点。

### 关键结论 / 决策摘要

- 无新增拍板（发版路线沿用既有 DECISIONS）；项目代码 `main` `f174b90`，冒烟加固随本次收尾提交。

### 遗留 / 交给下一台机器

- 发版后半程未走完：tag `v0.3.5` → GitHub Release 正式发布（Setup/blockmap/portable/latest.yml 四文件，文件名勿改）→ 托盘「检查桌面端更新」真机走完 0.3.5 自更新（历史事故现场复验）。
- [5] 可选加固：卸载后断言注册表卸载键也消失（当前已断言文件移除 + 真安装体完好）。
- 壳重开若弹错误页：3080 被全局 `dsh web` 占着，先关那个终端里的服务再开壳。
