# 2026-09-24 — B机

## 本次做了什么

- **[5] 误删真安装事故（本日主线）**：昨晚用户手动补跑 verify:smoke 输出 8/0/0
  全绿；今天起床发现桌面快捷方式消失、`D:\AI(CODEX)\dsh desktop\DS Harness Desktop`
  被卸空，被迫从 GitHub Release 重装自救（18:27:59）。取证定位罪魁 = [5] 自身，
  当天完成修复并提交推送（项目 `85a9d96`）。
- 证据链三条：① 旧 `stashUninstallKeys` 只扫 HKCU，本机全机安装的卸载键在 HKLM
  （探针 HKCU=0/HKLM=1），暂存扑空 → NSIS `uninstallOldVersion` 按 HKLM 找到真
  安装 → 静默跑真卸载器——项目注释 L403-405 写的就是这个事故形态；② 用户体检输出
  没有「已暂存」行 = `keyStash.length=0` 运行时自证；③ 两处 lnk CreationTime =
  09-24 18:27:59（GitHub 重装时刻）——NSIS 覆盖写不改 CreationTime，证明 23:17
  托盘升级创建的旧 lnk 在重装前已被删，窗口内唯一卸载类事件 = 昨晚 [5]。
- 取证中我犯的错（已向用户认错撤回）：拿 18:30 GitHub 重装**之后**的探针当
  "体检没删"的证据——大脑 git 时间戳 `70fc19a = 09-24 18:30:27` 钉死了探针时间。
  教训：**先钉时间戳再下结论；重装/修复之后的"完好"证明不了之前**。
- 第二个独立缺陷挂账：本机 Setup **双击交互路径** 0xc0000005（NSIS System.dll
  @0x1581，事件 ID1000 于 18:16/18:18 两次同偏移），静默 `/S` 路径正常——未根因。
- 修复（`fix(smoke)` +72/-38，`node --check` 过，已推送 `85a9d96`）：三 hive 暂存
  + fail-closed（导出/删除失败即中止）+ 键数组调用方持有（中途失败 finally 可导回
  已藏的键）+ 快照补公共桌面/所有用户开始菜单 + `realDirs` 三 hive 推导使「真安装
  体完好」成为真断言 + 0 暂存显式日志。

## 关键结论 / 决策摘要

- 新增 DECISIONS「[5] 防误删加固」条（详见该文件）。
- 核心教训：**体检输出绿 ≠ 真绿——三道断言全在空转（`realDirs=[]` 循环没执行、
  快照没盖全机位置、「已还原」无条件打印）就是假绿**；保护性步骤的作用域必须与
  真实环境对齐（本机 = 全机/HKLM），跑完要核对关键断言行真的打印过。

## 遗留 / 交给下一台机器

- 用户手动重跑 verify:smoke 验证修复（关桌面端 + 管理员终端），预期新增
  「已暂存 N 个（真实安装 D:\…）」「真实安装体完好（D:\…）」两行真断言；
  未提权会 fail-closed 报红（那也是正确行为，红=安全）。
- 本机 Setup 双击崩溃 0xc0000005 待根因（已挂 NEXT 下一步第 1 条，注意复现前
  先暂存卸载键，别裸双击）。
- 旧账照旧：低价值清账、[5] 卸载键消失断言、quarantine 隔离区勿回迁。
