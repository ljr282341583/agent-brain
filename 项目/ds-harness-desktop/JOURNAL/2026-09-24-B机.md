# 2026-09-24 — B机

## 本次做了什么

- **[5] 误删真安装事故（本日主线）**：昨晚用户手动补跑 verify:smoke 输出 8/0/0
  全绿；今天起床发现桌面快捷方式消失、`D:\AI(CODEX)\dsh desktop\DS Harness Desktop`
  被卸空，被迫从 GitHub Release 重装自救（18:27:59）。取证定位罪魁 = [5] 自身，
  当天完成修复并提交推送（项目 `85a9d96`）。
- 证据链三条：① 旧 `stashUninstallKeys` 只扫 HKCU，本机全机安装的卸载键在 HKLM
  （探针 HKCU=0/HKLM=1），暂存扑空 → NSIS `uninstallOldVersion` 按 HKLM 找到真
  安装 → 静默跑真卸载器——项目注释 L403-405 写的就是这个事故形态；② 用户体检输出
  没有「已暂存」行 = `keyStash.length=0` 运行时自证；③ 两处 lnk CreationTime =
  09-24 18:27:59（GitHub 重装时刻）——NSIS 覆盖写不改 CreationTime，证明 23:17
  托盘升级创建的旧 lnk 在重装前已被删，窗口内唯一卸载类事件 = 昨晚 [5]。
- 取证中我犯的错（已向用户认错撤回）：拿 18:30 GitHub 重装**之后**的探针当
  "体检没删"的证据——大脑 git 时间戳 `70fc19a = 09-24 18:30:27` 钉死了探针时间。
  教训：**先钉时间戳再下结论；重装/修复之后的"完好"证明不了之前**。
- 第二个独立缺陷挂账：本机 Setup **双击交互路径** 0xc0000005（NSIS System.dll
  @0x1581，事件 ID1000 于 18:16/18:18 两次同偏移），静默 `/S` 路径正常——未根因。
- 修复（`fix(smoke)` +72/-38，`node --check` 过，已推送 `85a9d96`）：三 hive 暂存
  + fail-closed（导出/删除失败即中止）+ 键数组调用方持有（中途失败 finally 可导回
  已藏的键）+ 快照补公共桌面/所有用户开始菜单 + `realDirs` 三 hive 推导使「真安装
  体完好」成为真断言 + 0 暂存显式日志。

## 关键结论 / 决策摘要

- 新增 DECISIONS「[5] 防误删加固」条（详见该文件）。
- 核心教训：**体检输出绿 ≠ 真绿——三道断言全在空转（`realDirs=[]` 循环没执行、
  快照没盖全机位置、「已还原」无条件打印）就是假绿**；保护性步骤的作用域必须与
  真实环境对齐（本机 = 全机/HKLM），跑完要核对关键断言行真的打印过。

## 遗留 / 交给下一台机器

- 用户手动重跑 verify:smoke 验证修复（关桌面端 + 管理员终端），预期新增
  「已暂存 N 个（真实安装 D:\…）」「真实安装体完好（D:\…）」两行真断言；
  未提权会 fail-closed 报红（那也是正确行为，红=安全）。
- 本机 Setup 双击崩溃 0xc0000005 待根因（已挂 NEXT 下一步第 1 条，注意复现前
  先暂存卸载键，别裸双击）。
- 旧账照旧：低价值清账、[5] 卸载键消失断言、quarantine 隔离区勿回迁。

## 加固第二批（同日续，项目 `3f26481`）

- 用户追问"你确定这次真不会删？"——不靠承诺，补两道**独立**防线并实测拦截：
  ① 硬闸 `assertNoVisibleUninstallKeys`：暂存后、动安装器前，用**独立实现**（PowerShell
  注册表提供程序，与暂存的 reg.exe 解析互为对照）复查三 hive，扫描失败/无输出或仍有键
  一律 fail-closed 中止；② `--audit-stash` 零风险自检：只演练 扫描 → 暂存 → 复查 →
  还原，不启动任何安装器。
- **自检实测（无提权会话）**：扫描精准命中唯一真键
  `HKLM\...\Uninstall\99d3b161-…`；暂存到"删 HKLM 键"那步被拒绝访问 → fail-closed
  中止 → 复核卸载键/记忆键/应用 exe 三者完好。**同一场景昨天是"静默卸载 + 8/0/0 绿"，
  今天是"测试自己报红、什么都没动"。**
- 补漏：同一 GUID 也是**安装记忆键**（`INSTALL_REGISTRY_KEY` = `Software\99d3b161-…`），
  temp 安装会覆盖它、temp 卸载器会删掉它 → 现在与卸载键**一并暂存/还原**；否则 [5]
  跑完会丢安装记忆 → 下次更新向导失去"原地回填"依据 → 漂移/双安装。
- 顺手修掉自研 bug：PowerShell 5.1 对**单元素数组** `ConvertTo-Json` 会退化成裸 JSON
  字符串（`"HKLM\…"`）→ 原解析判"扫描失败"；改成哨兵行协议（`KEY=<path>` +
  `SCAN-DONE <count>`，缺哨兵或计数不符即视作失败）。
- **新环境要求**：完整 [5] 必须用**管理员** PowerShell（暂存 HKLM 键要提权）；未提权会
  fail-closed 报红（红 = 安全）。用户上次能跑通是因为旧代码压根没碰 HKLM。
- 现成恢复手段：`产出\reg-backup-20260924\*.reg`（卸载键 + 记忆键手工导出）+ `reg import`；
  若硬关窗口导致键停在已暂存状态，用它一条命令复原。

## 加固第三批（同日续，项目 `a605bf3`）——快捷方式"零静默损失"

- 用户追问"确定不会删掉我的桌面快捷方式吗"→ 复查又抓到一个静默隐患：`restoreShortcuts`
  的失败路径只 `catch` 掉、最后却**无条件**打印"快捷方式已还原为安装前状态"（与昨天假绿同病）。
- 修复：① `restoreShortcuts` 返回"未能原样还原"清单并**逐个复查**快照路径在位；② [5]
  跑前记录**卸载键基线**、跑后以独立实现扫描对比；③ 收尾若快捷方式缺项或卸载键数未回基线，
  主流程成功时**直接判红**（附恢复指引）——不再有静默的"已还原"。
- 实况：公共桌面 lnk 与 ProgramData 开始菜单 lnk 均在原位；无提权自检回归通过（仍在删键处
  fail-closed 中止，键与 lnk 完好）。

## 加固第四批（设计级：把危险动作移出默认路径，项目 `ee518a8`）

- **默认安全化**：新增"第一优先守卫"——本机三 hive 存在该应用的卸载键（或扫描失败无法
  确认）→ [5] **默认 SKIP** 并打印原因与两条出路；要跑必须显式 `--force-installer`（护栏
  启用并打印警示）。本机实测：`npm run verify:smoke` → **通过 7、失败 0、跳过 1**，
  [5] 的 SKIP 理由正是"本机存在真实安装（1 个已登记卸载键…）"。
- **模板取证（决定性）**：`assistedInstaller.nsh` L112-120——安装模式由**安装记忆键**决定
  （HKLM 有 InstallLocation → 全机；HKCU 有 → 每用户；都没有 → 按构建默认）。这解释了事故
  当晚 temp 安装为何走全机（当时 HKLM 记忆键还在），也说明新加的"记忆键暂存"会顺带让它
  退回每用户。L129-133 确认 `/currentuser`、`/allusers` 参数被支持。
- **接触面隔离**：temp 安装现在带 `/currentuser` → 只写 HKCU 与用户级快捷方式，不碰公共
  桌面/HKLM；并新增观测行打印 temp 实例实际注册到哪个 hive（含 HKLM 即高亮告警）——
  隔离是否成立当场可见，不靠假设。
- 仍未消除的部分：产品名/GUID 相同意味着**同名 lnk 与同名键**依然存在于每用户命名空间，
  只是本机真实安装是全机、两者不重叠；**彻底消除需换 appId 的测试构建**（已挂 NEXT 可选项）。
