# DECISIONS — 已拍板的方案 / 技术决策

> 只追加，不改写历史（被后条取代时在旧条目末尾追加 ⚠️ 取代标注，属追加非改写）；“讨论中 / 待确认”的内容只进 JOURNAL，不进本文件。

## 2026-09-22 建立多机协作基线：origin/main 为唯一事实源 + 大脑仓库交接

- **决定**：代码只认 GitHub `origin/main`；A机 以 `G:\ai\ds-harness-desktop`
  （本次新克隆）作为长期开发副本，会话工作区那份只当次使用；跨机的开发思路 /
  决策 / 断点只写大脑仓库 `项目\ds-harness-desktop\`，开工前先读笔记再动手。
- **理由**：A/B 两台机器代码靠 git 已能同步，但思路此前无处沉淀；本次实测 A机
  克隆与远端完全一致（`88091a6`），具备直接接力条件。
- **否决**：继续把会话工作区那份克隆当长期副本（路径深、且属主不符导致每次跑 git
  都要额外加 `safe.directory`）。

> ⚠️ 本条中"长期副本 = `G:\ai\ds-harness-desktop`（新克隆）"已被同日「纠正：不另建克隆，唯一工作副本就是原项目文件夹」条目取代；"origin/main 唯一事实源 / 笔记进大脑仓库"部分仍然有效。

## 2026-09-22 纠正：不另建克隆，唯一工作副本就是原项目文件夹

- **决定**：项目仓库只用原位置 `G:\ai\deepseek harness output\workspace\projects\ds-harness-desktop`
  （已 `fetch`/`pull` 确认 `main` `88091a6` = 远端最新、工作区干净）；跨机笔记照旧进大脑仓库本目录。
- **理由**：那份本来就是完整克隆、内容干净且已是最新，"另克隆一份"不产生任何新东西；
  `dubious ownership` 报错用 `git -c safe.directory=<路径>` 即可绕过，不构成搬家理由。
- **否决**：保留 `G:\ai\ds-harness-desktop` 当"长期副本"（同日核实无改动后已删除）；
  以及"`G:\ai\` 是约定的仓库根"这一说法——查证后仅两个孤例，无任何成文约定。
