# DECISIONS — 已拍板的方案 / 技术决策

> 只追加，不改写历史（被后条取代时在旧条目末尾追加 ⚠️ 取代标注，属追加非改写）；“讨论中 / 待确认”的内容只进 JOURNAL，不进本文件。

## 2026-09-22 建立多机协作基线：origin/main 为唯一事实源 + 大脑仓库交接

- **决定**：代码只认 GitHub `origin/main`；A机 以 `G:\ai\ds-harness-desktop`
  （本次新克隆）作为长期开发副本，会话工作区那份只当次使用；跨机的开发思路 /
  决策 / 断点只写大脑仓库 `项目\ds-harness-desktop\`，开工前先读笔记再动手。
- **理由**：A/B 两台机器代码靠 git 已能同步，但思路此前无处沉淀；本次实测 A机
  克隆与远端完全一致（`88091a6`），具备直接接力条件。
- **否决**：继续把会话工作区那份克隆当长期副本（路径深、且属主不符导致每次跑 git
  都要额外加 `safe.directory`）。

> ⚠️ 本条中"长期副本 = `G:\ai\ds-harness-desktop`（新克隆）"已被同日「纠正：不另建克隆，唯一工作副本就是原项目文件夹」条目取代；"origin/main 唯一事实源 / 笔记进大脑仓库"部分仍然有效。

## 2026-09-22 纠正：不另建克隆，唯一工作副本就是原项目文件夹

- **决定**：项目仓库只用原位置 `G:\ai\deepseek harness output\workspace\projects\ds-harness-desktop`
  （已 `fetch`/`pull` 确认 `main` `88091a6` = 远端最新、工作区干净）；跨机笔记照旧进大脑仓库本目录。
- **理由**：那份本来就是完整克隆、内容干净且已是最新，"另克隆一份"不产生任何新东西；
  `dubious ownership` 报错用 `git -c safe.directory=<路径>` 即可绕过，不构成搬家理由。
- **否决**：保留 `G:\ai\ds-harness-desktop` 当"长期副本"（同日核实无改动后已删除）；
  以及"`G:\ai\` 是约定的仓库根"这一说法——查证后仅两个孤例，无任何成文约定。

## 2026-09-23 收尾自动打包+体检（档位二）

- **决定**：本项目「收尾」在提交推送后自动执行 `app` 下 `npm run build`（electron-builder 完整打包 + 6 项冒烟体检），全绿才可交付产物；该步骤已写入 `session-handoff` skill v1.0.4「第 6.5 步」（激活版与 brain 分发版同步改）。
- **理由**：用户不想每次手动打包；链路已在 A机 实测通过——NSIS 安装包 + 便携版产出、体检 6/6 绿；本次顺带修复打包链路存量 bug（Node 24 禁止裸 spawn `npm.cmd`，afterPack 回退改走 `node + npm-cli.js`，并补打 `result.error` 诊断）。
- **否决**：GitHub Actions 云端打包（档位三）暂不搭建，需要时另立决策。

## 2026-09-23 verify:smoke 五步体检挂进打包命令（发版门禁）

- **决定**：`npm run build` / `build:dir` 末尾自动执行五步冒烟（离线单测 / 静态检查 / 侧车启动 / win-unpacked 启动 / 安装器端到端），`失败 0` 即全绿、报红不得发布；[5] 检测到正式实例在跑自动 SKIP 防连坐（`--force-installer` 可强行）；验证隔离靠三个仅 env 生效开关（`DSH_DESKTOP_PORT` / `DSH_DESKTOP_USER_DATA` / `DSH_DESKTOP_SMOKE`）。
- **理由**：用户多次"更新完打不开/闪退只能找人救"，项目无 CI 也无其它自动化门禁；把反正要人点的检查固化成一条命令，实测完整链路通过。
- **否决**：维持纯人工 `npm start` 冒烟（覆盖低、靠记性）。

## 2026-09-23 档位二适用范围仅限 ds-harness-desktop

- **决定**：session-handoff skill 第 6.5 步按项目名写死；其他项目收尾只存笔记不打包。
- **理由**：各项目打包命令不同或不存在，通用钩子会对无构建项目报错；本项目是当前唯一有分发产物的。
- **否决**：改成"看大脑笔记约定执行"的通用钩子（用户选定维持现状，将来要开再改）。

## 2026-09-23 开发模式：维持 desktop（省事路线）

- **决定**：日常继续用 desktop 当开发环境；[5] 靠守卫跳过保安全；需要全量 [5] 实证时（发版前）由用户关壳手动跑一次命令再重开。
- **理由**：守卫已实测杜绝连坐误杀；会话数据在 `~/.dsh`，即便壳出事也不丢历史。
- **否决**：切换网页版开发（其优点已分析：壳可牺牲、[5] 每次全覆盖，本次不迁移，需要时再启用）。

## 2026-09-23 更新反馈三件套

- **决定**：桌面端自更新补三层反馈——下载完成弹窗（含「立即重启并安装」直达，复用
  `applyShellInstall` 公共函数并加 `quitting` 防连点双装）、下载失败弹窗（silent 调用
  不弹）、下载中 busy 文案改为「正在下载桌面端 vX…」；进度反馈保留既有任务栏进度条 +
  托盘悬停百分比。
- **理由**：用户实测 0.3.5 更新时"没有进度条也没有完成提示"，代码证实完成仅改托盘菜单
  文字、失败只写日志——反馈缺口是真缺陷；单测 24/24、`node --check` 通过。
- **否决**：加码做窗口内进度条（需动 renderer，工作量中等，需要时另拍）；维持零弹窗的
  静默设计。

## 2026-09-23 安装位置回归 G:\ 且保持全机模式

- **决定**：把 0.3.5 从 `C:\Program Files\DS Harness Desktop` 搬回用户原目录
  `G:\ai\dsh desktop\DS Harness Desktop`，执行时**保持全机（/allusers）模式**，使
  记忆键 `HKLM\Software\99d3b161-…\InstallLocation` = G:\；今后更新按记忆键原地回填 G:\。
- **理由**：用户明确要求回原目录；全机模式是唯一不产生双安装的路径（卸载器只认同注册表
  侧，切 currentuser 会让 Program Files 那份成孤儿）；记忆键回填后默认值即 G:\，漂移
  触发点（向导模式页被切）不再具备——终验卸载键/双快捷方式/记忆键三处全指 G:\。
- **否决**：切「仅当前用户」装回 HKCU（留孤儿副本与双卸载入口）；留在 Program Files
  （违背用户要求）。

## 2026-09-23 0.3.6 打包门禁升级：build 自动前置 prepare-runtime + 侧车 npm 断言

- **决定**：`npm run build`/`build:dir` 前置自动执行 `app\scripts\prepare-runtime.ps1`
  （脚本加幂等快路径：runtime 与 npm 就绪且 `node --version` 吻合时直接 exit 0，已就绪
  时秒过不重下）；verify:smoke [2] 静态检查新增「侧车含可执行 npm」断言（npm-cli.js
  存在 + 侧车真跑 `--version` 出 `x.y.z`）。版本升 0.3.6。
- **理由**：本地打包忘跑 prepare-runtime 会拿缺 npm 的 runtime 出货，与 CI 产物不一致；
  该条本是 NEXT 可拍板小项，B机 09-23 升号时一并落地；双 README 同步说明防误踩。
- **否决**：维持「人工打包前记得手动跑脚本」的口头约定（依赖记性，NEXT 原条目已拍掉）。

## 2026-09-23 损坏会话处置：隔离陈旧 v2，不回迁

- **决定**：把 2 个户口失配的陈旧 v2 会话文件（b319e52f、cde953a9 的
  `session.jsonl.zstd`）移出 sessions 树至
  `C:\Users\Administrator\.dsh\quarantine-legacy-v2\`（保留原相对路径结构），v3 当代
  文件原地不动；复刻 `assertStoredIdentity` 路径推导做离线审计复验 82/82 全绿。
- **理由**：项目文件夹改名 + 09-19 目录批量搬迁只留下 v2 旧代 header 的老户口，v3
  户口与位置本就一致；0.1.5 启动 `listArtifacts` 只选最高代校验，v2 永不被读——隔离后
  存储树对任意版本读取器都自洽，历史一字未改、可按原路径找回。
- **否决**：① 整会话目录搬回老户口目录（活的 v3 变错位 → 启动 listArtifacts 必崩）；
  ② 只把 v2 搬回老户口目录（同 id 出现在两个项目目录 → duplicate id 必崩）；
  ③ 改写 v2 header 户口（动文件内容，风险大于收益）。最初拍板的「挪回原位」被 ①② 的
  源码链路与离线审计证伪后改道为隔离。

## 2026-09-24 [5] 防误删加固：三 hive 暂存 + fail-closed + 全机快照

- **决定**：verify:smoke [5] 卸载键暂存改扫 HKCU / HKLM / HKLM-WOW6432Node 三
  hive，导出或删除失败（典型 = 未提权）立即抛错中止；键数组由调用方持有，中途
  失败 finally 仍能导回已藏的键；快捷方式快照补公共桌面与所有用户开始菜单；
  `realDirs` 由三 hive 推导使「真安装体完好」成为真断言，0 暂存时显式打印。
- **理由**：2026-09-24 实测事故——全机安装的键在 HKLM、旧实现只扫 HKCU，暂存
  扑空后 NSIS uninstallOldVersion 把真安装当旧版静默卸载（D:\ 目录、公共图标、
  HKLM 键全没），且三道断言全部空转输出 8/0/0 假绿；保护步骤的作用域必须与真实
  环境对齐。修复提交 `85a9d96`。
- **否决**：① 直接删掉 [5]（丢掉唯一安装器 E2E）；② 只加警告日志继续跑（警告
  挡不住事故，必须 fail-closed）；③ 只改注释/文档提醒（本次事故恰是"文档写了、
  实现没跟上"的形态）。

## 2026-09-24 [5] 第二道防线：独立复查硬闸 + 零风险自检 + 记忆键一并暂存

- **决定**：在三 hive 暂存（`85a9d96`）之上再加一层：① 动安装器前用**独立实现**
  （PowerShell 注册表提供程序，与暂存的 reg.exe 解析互为对照）复查三 hive，扫描
  失败/无输出或仍有键一律 fail-closed 中止；② 新增 `--audit-stash` 零风险自检
  （只演练 扫描 → 暂存 → 复查 → 还原，不启动任何安装器）；③ **安装记忆键**
  （`INSTALL_REGISTRY_KEY` = `Software\<GUID>`）与卸载键一并暂存/还原；④ 完整
  [5] 需**管理员** PowerShell（暂存 HKLM 键要提权）。提交 `3f26481`。
- **理由**：用户要求"真不会删"的证据而非承诺——无提权会话实测：扫描精准命中唯一真键、
  删除被拒时 fail-closed 中止、卸载键/记忆键/应用 exe 三者完好（同一场景旧代码下是
  静默卸载 + 8/0/0 假绿）；记忆键若不暂存，[5] 跑完会丢安装记忆，下次更新向导失去
  原地回填依据（漂移/双安装）。
- **否决**：① 只靠"暂存逻辑自身正确"（同一段代码既当防线又当验证，出事无兜底）；
  ② 用 JSON 回传扫描结果（PS 5.1 单元素数组会退化成裸字符串，已改哨兵行协议）；
  ③ 不要求提权、无提权时"尽力而为"（那正是事故形态）。
## 2026-09-24 [5] 默认安全化：本机有真实安装即 SKIP，temp 安装强制每用户命名空间

- **决定**：① verify:smoke [5] 在"本机三 hive 存在该应用卸载键"或"扫描失败无法确认"时
  **默认 SKIP**，发版门禁由 CI（干净机器）承担；确要本机跑须显式 `--force-installer`
  （护栏启用并打印警示）。② temp 安装固定传 `/currentuser`，强制每用户命名空间，并打印
  temp 实例实际注册的 hive 供当场核对。提交 `ee518a8`。
- **理由**：设计级根因——NSIS 的 lnk 路径与注册表键名由产品名/GUID 派生、与安装目录无关，
  "装到临时目录"隔离不了命名空间；`assistedInstaller.nsh` L112-120 显示安装模式由安装
  记忆键决定（事故当晚 HKLM 记忆键在 → temp 走全机 → 读写公共桌面/HKLM 并触发真卸载器）。
  安全关键系统优先消除危险默认路径，补偿性护栏只作纵深防御。
- **否决**：① 只保留护栏、维持"默认就跑 [5]"（危险动作仍在默认路径上）；② 直接删掉 [5]
  （丢掉唯一安装器 E2E）；③ 继续沿用"装到 temp 即隔离"的旧假设。
## 2026-09-24 [5] 消除级隔离：体检专用「隔离命名」构建（build:smoketest）

- **决定**：新增 `npm run build:smoketest`（`app/scripts/build-smoketest.js`）——体检用安装包
  把 `productName` / `nsis.shortcutName` / `nsis.guid` 三样一起换成 SmokeTest 专属值，产物落
  `产出\smoketest\` 并写 `smoketest.json`；`verify:smoke` 读到清单即进入隔离模式：用隔离包、
  按自有 GUID 核对注册、**跳过真实安装键暂存**（因此不再需要提权）、跑前跑后逐项核对真实
  安装的键清单与快捷方式。提交 `b61b7d3`。
- **理由**：`NsisTarget.js` L147 显示键名 = `UUID.v5(appId)`、L434 显示快捷方式名由
  `shortcutName` 决定——**命名空间隔离靠"换名"，不是靠"换目录"**。实测（本机有真实安装 +
  4 实例在跑 + 无提权）：隔离包装/启/卸全绿 8/0/0，真实安装的注册表键清单与快捷方式全程
  未被触碰。这是"消除危险"而非"补偿危险"，也是唯一不依赖"还原必须成功"的方案。
- **否决**：① 只靠暂存+还原护栏（安全前提是还原成功，留时间窗）；② 只靠 `/currentuser`
  （只改"写哪儿"，同名 lnk/键在每用户命名空间依然存在，仍有潜在重叠）；③ 放弃本机 [5]
  覆盖（隔离构建已能在本机安全跑，无需牺牲）。

## 2026-09-25 应用内 dsh 更新回退 bug：1 行身份守卫 + 先热补丁已安装体

- **决定**：`app/src/main.js` 的 `child.on('exit')` 内加 `if (dshChild !== child) return;`
  （置于关日志 fd 之后、`dshChild = null` 之前）。除改仓库源码外，**同步对已安装体**
  （`…\resources\app\src\main.js`；asar: false，可直接改）打同一补丁让修复当天生效，
  随后发 v0.3.7 由正式产物覆盖。
- **理由**：旧进程 exit 被误判为「覆盖版本启动失败」，覆盖版本从未被拉起过（四次同因）。
  守卫置于 close fd 之后是为了不漏关日志 fd（Windows 上会挡住日志轮转）；`dshExitPromise`
  用独立的 `child.once('exit')`，故提前 return 不会让「安装前等待子进程退出」挂起。
  选热补丁而非只发版：发版链路长（打包 + CI），而用户当天正卡在这个 bug 上。
- **否决**：① 改 `restarting` 布尔标志（多点置位易漏，身份比对是唯一不会漏的判据）；
  ② 只发版不热补丁（要多等一轮发版才能用上 dsh 0.1.7）。

## 2026-09-25 发布 v0.3.7：tag 注释避开 `#` 开头

- **决定**：版本 0.3.6 → 0.3.7，本地 `npm run build` 验证（通过 7 / 失败 0 / 跳过 1）后提交
  `bdf0bb8`、打 tag `v0.3.7` 交 CI 发布；发版说明的标题**一律用 `**粗体**`**，不用 `#`/`##`。
- **理由**：CI 取 tag 注释作 Release 正文，而 `#` 开头的行会被 git 从注释里剥掉（0.3.5/0.3.6
  踩过、标题全丢），`git tag -F` 也照剥。实测本轮 34 行进 / 34 行出，四节标题完整写回；
  CI 全绿，正式 Release 四资产齐全、线上 `latest.yml` = `version: 0.3.7`。
- **否决**：只在 Releases 页面手工贴说明（CI 会用 tag 注释覆盖，前几版说明就是这么丢的）。

## 2026-09-25 插件升级不绕过 pnpm 供应链冷却期

- **决定**：`dshmarket` / provider 只升到 pnpm 当前允许的版本（1.58.0 / 0.11.11），**不**把它们
  写进 `pnpm-workspace.yaml` 的 `minimumReleaseAgeExclude` 去抢装 1.65.1 / 0.11.14，等满 1 天再升。
- **理由**：pnpm 11 默认 `minimumReleaseAge=1440`（1 天）是防投毒的供应链保护、属有意默认；
  排除名单是逃生门，为"追最新"常开就把它变成常态。本轮也无必须立刻升的理由（0.1.7 的兼容
  修复早在 0.11.9 就具备）。
- **否决**：为追最新版把这两个包加进 `minimumReleaseAgeExclude`（把安全默认当障碍）。

## 2026-09-25 权限预设 defaultPreset 取保守值 workspace-write

- **决定**：`profiles\web\cordis.patch.yml` 的 `defaultPreset` 由 `auto` 改为 `workspace-write`
  （三个 presets 原样保留，含自定义的 `read-only`）。
- **理由**：0.1.7 里 `auto` 是 Auto 审阅集成保留的运行时身份，而构造函数即 `resolve`、
  此时集成必然未挂载 → 必定 `unknown preset "auto"`、该条目激活失败；`auto` 也不允许出现在
  用户 presets 表里。`workspace-write` 是有效值中较保守的一个，与会话初始态一致。
- **否决**：① 删掉 `defaultPreset` 让引擎自动推断（推断值若落在 presets 表外会抛 `custom` 错）；
  ② 默认 `danger-full-access`（security-relevant 的默认不该由 agent 擅自放宽）。

## 2026-09-25 与 agent-loop 脱钩：项目规则改由 AGENTS.md 自持

- **决定**：本项目**不再使用 `agent-loop`**——不加载 agent-loop skill、不设 Stage / Gate / artifact
  流程件、**废弃外部记忆根**（另一工作区 `projects/dsh-desktop-updater/.agent-loop/`）。项目规则
  全部改由仓库根 `AGENTS.md` 自持：工作方式 / 记忆与交接 / 验证与提交 / 架构快照 / 目录指引 /
  项目命令 / 8 条硬约束；`CLAUDE.md` 只作指针。
- **理由**：实际工作路径早已是「查证 → 改 → `npm run build` 体检 → 用户点头才提交」，仓库内从未
  产出 Gate/artifact 类流程件（确认无 `.agent-loop/`、无 `requirements/`）；而规则源在仓库之外，
  改一条约束要跨工作区改源再同步，已实际漂移——命令表过时（写「无自动化测试」）、硬约束 3 与现实
  不符（`prepare-runtime.ps1` 会补 npm），本次一并修正。
- **替代安排**：跨机记忆照旧本大脑目录（`CONTEXT.md` / `NEXT.md` / `DECISIONS.md` / `JOURNAL\`），
  本机按次细节写仓库 `过程记录/`；收尾仍走 `session-handoff` skill；提交 / 推送 / tag / 发版仍需
  用户明确授权。
- **否决**：① 保留 agent-loop 但降级为可选（规则源仍在仓库外，漂移会复发）；② 只删 managed block
  不补新规则（留下无规则状态）；③ 顺手删 `@deepseek-ai/dsh-agent-loop` 依赖（那是 dsh 官方插件包，
  与工作流无关）。
- **落地**：项目仓库 `AGENTS.md` 重写、`CLAUDE.md` 改指针。
